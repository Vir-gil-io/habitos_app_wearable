name: habitos_app_wearable
description: "HabitFlow — Dashboard informativo para Wear OS"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.16.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.5
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
